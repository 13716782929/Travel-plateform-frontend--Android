<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#536DFE" /> <!-- Darker shade for press -->
            <corners android:radius="16dp" />
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape>
            <solid android:color="#448AFF" /> <!-- Default background color -->
            <corners android:radius="16dp" />
        </shape>
    </item>

</selector>
